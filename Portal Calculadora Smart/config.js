/* Configuración central. Propuesta en REVISIÓN, sin destinos de producción.
 * Sustituir cada cadena vacía por su URL absoluta https:// autorizada.
 * LINK_TARGET: "_self" (misma pestaña), "_top" (salir del iframe)
 *              o "_blank" (nueva pestaña).
 * No incluir credenciales ni datos personales en estas direcciones.
 */
window.SMART_CONFIG = Object.freeze({
  INSTITUTO_URL: "",
  ONLINE_FLEX_URL: "",
  SMART_MIX_URL: "",
  LINK_TARGET: "_self"
});
